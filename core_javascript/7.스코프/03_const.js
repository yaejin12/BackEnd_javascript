//const : 상수 선언(값을 바꿀수 없다)
const y = "메롱";
console.log(y);

/*
$div.style.background = '#0f0'
*/

//상수는 객체의 불변성을 유지할 수 있다.
//객체를 선언할때는 const를 사용하는 것이 좋다.
let kim = {
  name: "김철수",
  age: 30,
};

//배열도 사실 객체의 일부이다.
